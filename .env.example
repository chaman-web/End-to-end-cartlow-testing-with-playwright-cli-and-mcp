# ── Environment ────────────────────────────────────────────────────────────────
# Copy this file to .env and fill in your values.
# Load with: export $(cat .env | xargs) before running pytest.
# Or use python-dotenv: pip install python-dotenv

ENV=staging                          # staging | stage2 | production

# ── Credentials ────────────────────────────────────────────────────────────────
TEST_EMAIL=muhammad.akmal@cartlow.com
TEST_PASSWORD=Test!123

# ── Browser ────────────────────────────────────────────────────────────────────
HEADLESS=true                        # true | false (false = headed mode)
SLOW_MO=0                            # milliseconds between actions (0 = fastest)

# ── Database (optional — only for DB assertion tests) ──────────────────────────
DB_HOST=209.38.211.128
DB_PORT=3306
DB_NAME=cartlow_dev
DB_USER=
DB_PASS=
